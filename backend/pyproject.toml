[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q --tb=short"

[tool.coverage.run]
source = ["."]
omit = [
    "tests/*",
    "scratch/*",
    "venv/*",
    "__pycache__/*",
]

[tool.coverage.report]
fail_under = 60
show_missing = true
skip_empty = true
